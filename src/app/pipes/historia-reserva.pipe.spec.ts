import { HistoriaReservaPipe } from './historia-reserva.pipe';

describe('HistoriaReservaPipe', () => {
  it('create an instance', () => {
    const pipe = new HistoriaReservaPipe();
    expect(pipe).toBeTruthy();
  });
});
