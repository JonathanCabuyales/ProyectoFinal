import { DevolverArregloPipe } from './devolver-arreglo.pipe';

describe('DevolverArregloPipe', () => {
  it('create an instance', () => {
    const pipe = new DevolverArregloPipe();
    expect(pipe).toBeTruthy();
  });
});
