import { FotosPropPipe } from './fotos-prop.pipe';

describe('FotosPropPipe', () => {
  it('create an instance', () => {
    const pipe = new FotosPropPipe();
    expect(pipe).toBeTruthy();
  });
});
